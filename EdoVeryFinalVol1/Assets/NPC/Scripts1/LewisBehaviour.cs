using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LewisBehaviour : MonoBehaviour
{
    Animator lewisAnimator;
    // Start is called before the first frame update
    void Start()
    {
        lewisAnimator = GetComponent<Animator>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
